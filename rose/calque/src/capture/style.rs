//! Design-token extraction from computed styles — colours, fonts, spacing.
//!
//! TODO: evaluate JS in the page to read computed styles off the rendered tree,
//! then cluster into a token set the React build can consume (e.g. a Tailwind
//! theme). Stubbed until the DOM/a11y inventory lands to anchor it.
