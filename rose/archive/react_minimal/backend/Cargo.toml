[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
ntex = { version = "2", features = ["tokio"] }
ntex-files = "2.0.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.133"
