# Pure

This module is the home for utilities that don't have any dependencies other than the Python standard library.

Therefore this will most likley end up being the most chaotic, but will always be installable without any additional dependencies.

There's networkx as this is a pure python library so it's a extremely low risk dependency, but might be moved in the future.
