# Résultat éphémère du run de benchmark courant (le gate le compare à la
# baseline committée). La baseline, elle, est versionnée.
.benchmarks/current.json

# Working copy generated by mutmut (mutation testing).
mutants/

# Hypothesis example database (property testing).
.hypothesis/
